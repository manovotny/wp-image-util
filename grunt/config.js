module.exports = function (grunt) {

    'use strict';

    grunt.initConfig({
        config: {
            version: {
                from: '0.3.1',
                to: '0.4.0'
            }
        }
    });

};
